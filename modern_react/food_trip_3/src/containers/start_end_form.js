import React, { Component } from 'react';
import { reduxForm } from 'redux-form';

class StartEndForm extends Component {
  render() {
    return (
      <input placeholer='start' />
    );
  }
}

export default StartEndForm;
